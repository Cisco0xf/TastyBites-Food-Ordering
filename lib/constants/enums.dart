enum Side {
  top,
  bottom,
  right,
  left,
  all,
}

enum From {
  horizontal,
  vertical,
  all,
}

enum Push {
  push,
  replace,
  clear,
  none,
}

enum RateFilter {
  any,
  medium,
  high,
}
